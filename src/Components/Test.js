import React from "react";
import { render } from "@testing-library/react";

function test() {
    const numbers = [1,2,3,4];
    render(
        return(
        list = numbers.map((number) => <l1>{number}</l1> )
        );
    )
}